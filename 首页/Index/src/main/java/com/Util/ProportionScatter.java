package com.Util;

import java.util.List;

import javax.servlet.http.HttpServletRequest;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import com.pojo.User;

/**
 * 
 * @author Administrator
 *	比例整理
 */
@Component
public class ProportionScatter {
	/*@Autowired
	ItemsProportion itemsProportion;
	
	public List<ProportionPojo> getProPorTion(HttpServletRequest request, User isNotUser){
		List<ProportionPojo> proportion = itemsProportion.Proportion(request, isNotUser);
		
		for (int i = 0; i < proportion.size(); i++) {
			for (int j = i; j < proportion.size(); j++) {
				if (!proportion.get(i).equals(proportion.get(j))) {
					//打散处理
				}
			}
		}
		
		
		return proportion;
	}*/
	
	
	
	
}
